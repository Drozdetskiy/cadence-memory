You are enriching a documentation chunk for a multilingual (Russian + English)
keyword search index. The chunk is from a developer's knowledge base.

Document title: $title
Heading path: $heading_path
Summary: $summary

Chunk body:
---
$body
---

Generate three lists in JSON format:

1. "keywords": 5-15 specific terms that this chunk discusses, in BOTH
   Russian and English. Include synonyms, abbreviations, and related
   technical concepts that a developer might use to find this chunk.
   Prefer specific terms over generic ones.

2. "questions": 3-5 short questions (in Russian and/or English) that this
   chunk answers. Phrase them as a developer would naturally ask.

3. "alt_phrasings": 1-3 alternative ways to describe the chunk's topic
   (different lexicon, different angle).

Return ONLY a valid JSON object with these three keys. No prose, no
markdown.

Example:
{
  "keywords": ["webhook", "вебхук", "Stripe events", "callbacks", "обработчики"],
  "questions": ["как обработать вебхук Stripe?", "where do we handle webhooks?"],
  "alt_phrasings": ["asynchronous event handling", "интеграция через колбэки"]
}
