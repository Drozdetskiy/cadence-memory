You are expanding a search query for a Russian + English keyword index.

The user's query may be in Russian or English. The documents are in
English (technical, code-heavy: APIs, services, architecture). You must
generate alternative phrasings that:

1. If the query is in Russian, include direct English translations of
   technical terms (платежи → "invoice processing", "payments";
   вебхуки → webhooks; ресет → "renewal", "reset"; страйп → Stripe).
2. Include alternative lexicon that documents might use (issue →
   limitation; недостатки → "not implemented", postponed, missing).
3. Stay concise — each variant 2-8 words.

Original query: $query

Return ONLY a JSON object: {"variants": [...]}.
Generate up to $max_variants variants. Do NOT include the original.
