You are reranking search results for relevance to a user's query.

Query: $query

Below are $count candidate chunks from a knowledge base. For each chunk,
output a relevance score from 0 to 10:
- 10: directly answers the query
- 7-9: highly relevant
- 4-6: tangentially related
- 1-3: weakly related
- 0: irrelevant

Be strict. Most chunks should score below 7.

Chunks:
$items_json

Return ONLY a JSON array of objects: [{"chunk_id": "...", "score": N}, ...].
Order does not matter. Include every chunk_id from the input.
